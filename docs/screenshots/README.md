Screenshots embedded in the top-level [README.md](../../README.md).

Expected filenames (drop PNGs in this directory to populate):

- `hero.png` — full app, waterfall + FFT + an active decoder panel (e.g. FT8 or NAVTEX)
- `decoders.png` — decoder variety (WEFAX image + SSTV + HFDL JSON, or similar collage)
- `iq-visualizers.png` — page-5 IQ visualizers (SFRC / DOPP / ZOOM)
- `mobile.png` — phone view showing the touch-first keypad UI

Suggested specs: PNG, 1600–2000 px wide, < 500 KB each (pngquant the
exports). For multi-image rows in README, use an HTML `<p align="center">`
block with side-by-side `<img>` tags — GitHub renders that cleanly.
