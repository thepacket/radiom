// Stub: fldigi's FLTK rich-text widget. cw.cxx references the styled-text
// constants when emitting characters; we map them to ints.
#pragma once
namespace FTextBase {
  enum TEXT_ATTRIBUTES { RECV, XMIT, CTRL, SKIP, ALTR, NATTR };
}
